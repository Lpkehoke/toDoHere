'use strict';

const React = require('react');

class CardExpandButton extends React.Component {
    render() {
        return (<a className="text-muted" href="#">Expand</a>);
    }
}

module.exports = CardExpandButton;
