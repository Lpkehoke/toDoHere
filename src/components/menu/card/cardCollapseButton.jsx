'use strict';

const React = require('react');

class CardCollapseButton extends React.Component {
    render() {
        return (<a className="text-muted" href="#">Collapse</a>);
    }
}

module.exports = CardCollapseButton;
